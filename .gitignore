.DS_Store
*.pyc
__pycache__/

# stato per-macchina del monitor MP (non condiviso)
brain/tools/mp_state.local.json

# epub dei romanzi: opere pubblicate, diritti ceduti — restano solo in locale
Romanzi/

# produzione scientifica: PDF di articoli/monografie con diritti editoriali —
# nel repo va solo il lavoro derivato (digest, note) in brain/ricerca/
Ricerca/

# lavori in corso: PDF/DOCX non versionati

# plugin WordPress Phitness Core (codice): non nel second brain
Phitness/
